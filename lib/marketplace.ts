export type Offer={id:string;title:string;store:string;price:number;currency:string;url:string;image:string;shipping:string;rating:number|null;position:number};
function https(value:unknown){try{const u=new URL(String(value));return u.protocol==='https:'?u.href:''}catch{return ''}}
export function normalizeOffers(data:any):Offer[]{const raw=[...(data.shopping_results||[]),...(data.inline_shopping_results||[]),...(data.categorized_shopping_results||[]).flatMap((g:any)=>g.shopping_results||[])];const seen=new Set<string>();return raw.flatMap((r:any,i:number)=>{const url=https(r.link)||https(r.product_link);const price=Number(r.extracted_price);if(!url||!r.title||!r.source||!Number.isFinite(price)||price<=0||!/(R\$|BRL)/i.test(String(r.price)))return[];const key=String(r.product_id||r.title)+'|'+r.source+'|'+price;if(seen.has(key))return[];seen.add(key);return[{id:key,title:String(r.title).slice(0,300),store:String(r.source).slice(0,100),price,currency:'BRL',url,image:https(r.thumbnail),shipping:typeof r.delivery==='string'?r.delivery.slice(0,200):'',rating:Number.isFinite(r.rating)?r.rating:null,position:i}]})}
